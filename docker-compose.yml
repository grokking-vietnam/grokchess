version: '3.9'
services:
  web:
    image: nginx:latest
    ports:
      - "80:80"
    # volumes:
    #   - ./nginx.conf:/etc/nginx/nginx.conf
    #   - ./html:/usr/share/nginx/html

  crawler:
    build:
      context: "./crawler"
      dockerfile: "Dockerfile"

  ingestor:
    build:
      context: "./ingestor"
      dockerfile: "Dockerfile"
    volumes:
      - ./html:/var/www/html
